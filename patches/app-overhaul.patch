diff --git a/app/build.gradle b/app/build.gradle
index e790c64..2d520e8 100644
--- a/app/build.gradle
+++ b/app/build.gradle
@@ -10,6 +10,9 @@ android {
         targetSdkVersion 28
         versionCode 32
         versionName "11.2"
+        ndk {
+            abiFilters 'arm64-v8a'
+        }
     }

     buildTypes {
@@ -17,9 +20,6 @@ android {
             minifyEnabled true
             proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'

-            ndk {
-                abiFilters 'arm64-v8a'
-            }
         }
     }

@@ -27,7 +27,7 @@ android {
         checkReleaseBuilds false
     }

-    ndkVersion '24.0.8215888'
+    ndkVersion '25.2.9519653'

     externalNativeBuild {
         cmake {
diff --git a/app/src/main/java/com/winlator/xenvironment/components/GuestProgramLauncherComponent.java b/app/src/main/java/com/winlator/xenvironment/components/GuestProgramLauncherComponent.java
index 84c4702..acf94cb 100644
--- a/app/src/main/java/com/winlator/xenvironment/components/GuestProgramLauncherComponent.java
+++ b/app/src/main/java/com/winlator/xenvironment/components/GuestProgramLauncherComponent.java
@@ -100,7 +100,13 @@ public class GuestProgramLauncherComponent extends EnvironmentComponent {
         envVars.put("BOX64_LD_LIBRARY_PATH", rootDir+"/lib/x86_64-linux-gnu");
         envVars.put("ANDROID_SYSVSHM_SERVER", rootDir+UnixSocketConfig.SYSVSHM_SERVER_PATH);

-        if (this.envVars != null) envVars.putAll(this.envVars);
+                if (this.envVars != null) envVars.putAll(this.envVars);
+
+        // Kirin 9000WL: reserve the four performance cores (CPU 4-7) for Box64.
+        envVars.put("BOX64_MAXCPU", "8-4,5,6,7");
+        // Maleoon 910 compatibility/performance defaults; these are mandatory.
+        envVars.put("GALLIUM_DRIVER", "panfrost");
+        envVars.put("TU_DEBUG", "noconform");

         File shmDir = new File(rootDir, "/tmp/shm");
         if (!shmDir.isDirectory()) shmDir.mkdirs();
